## Archived Pixelated Headshot

These files preserve the original pixelated headshot interaction so it can be reused later without digging through old homepage code.

- `ArchivedPixelatedProfileSection.tsx`
- `ArchivedRasterSplit.tsx`

`ArchivedPixelatedProfileSection.tsx` keeps the original overlay copy and interaction wiring.
`ArchivedRasterSplit.tsx` keeps the canvas-based reveal/splitting effect itself.
